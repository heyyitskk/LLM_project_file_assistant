# Part B (previous milestone): file-system tools + OpenRouter chat assistant
openai>=1.50.0
pypdf>=4.0.0
python-docx>=1.1.0

# This milestone: RAG resume matching
chromadb>=0.5.0
sentence-transformers>=3.0.0
pandas>=2.0.0
matplotlib>=3.8.0
jupyter>=1.0.0

# Dataset generation (used reportlab only to create the sample PDF resume)
reportlab>=4.0.0
